# 2026-09-11 실행 — 신규 템플릿 제안

## SIBLING_MATCHUP (가칭) — 형제/가족 대결 특화

오늘 경기 묶음 재료를 고르다가 `graph.json`의 라이벌 엣지(`50709 52060`)가 한화 박정현과
KT 박영현의 **친형제 관계**를 가리키는 것을 확인했다(각 위키 문서의 프로필 요약·별명·밈
섹션에 "친형 박정현"/"KT 위즈 투수 박영현과 형제 사이"로 명시). 현재는 이런 소재를 범용
`RELATION_LINK`(공통점 4지선다)로만 담을 수 있는데, 형제·가족 관계는 일반적인 "공통점"보다
정서적으로 더 강한 훅이라 별도 템플릿으로 분리하면 좋겠다:

- **kind**: KNOWLEDGE, **format**: BINARY 또는 OX (예: "한화 박정현과 KT 박영현은 친형제
  사이다 — O/X" 또는 "다음 중 형제 관계인 두 선수는?" 2지선다)
- **needs**: `graph`(관계 타입에 `형제` 신설 필요 — 현재 허용 타입은 밈공유·커리어교차·
  라이벌 셋뿐이라 이번 실행은 부득이 `라이벌`로 분류된 엣지를 형제 관계 근거로 재해석해
  썼다. 정확한 표현을 위해 `형제` 엣지 타입 신설을 검토할 만하다)
- **subjectScope**: PLAYER, playerIds 2명
- 오늘처럼 두 선수가 서로 다른 두 팀(오늘 다른 경기 묶음에 각각 소속) 소속일 때 공통 묶음
  EXPERT 슬롯에 배치하면 팀 편중 없이 자연스럽다.

카탈로그 반영은 사람 승인 후 수동으로 진행해야 한다(이 문서는 제안만 남긴다).
